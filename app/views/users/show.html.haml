=render template: "navbar/navbar"

.p=@user.username
.p=@user.email

- @user.pictures.each do |pic|
	%img{src:"#{pic.image.url}", style:"width:100px;"}

=simple_form_for(:picture, :url => { :controller => "users", :action => 'create_picture' }) do |f|
	= f.input :image, as: "file", input_html: {accept: "image/*"}
	= f.submit
	
%input{type: "radio"} Nature
%input{type: "radio"} Beautiful
%input{type: "radio"} Landscapes
%input{type: "radio"} Trees
%input{type: "radio"} Green
